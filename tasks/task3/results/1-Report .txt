изменения:

1. Добавление внешнего типа  в booking-subgraph

  extend type Hotel @key(fields: "id") {
    id: ID! @external
  }
  
  2. имплементация booking-subgraph-> resolvers-> booking
  3. имплементация booking-subgraph-> resolvers-> ACL - валидация параметра context.req?.headers['userid'] 
  4. добавление в резолверы booking-subgrap : hotel
  
  hotel: (booking) => {
      return { __typename: 'Hotel', id: booking.hotelId };
    },
	
	5. Имплементация booking-subgraph->bookingsByUser c ACL
	 
	 if (req?.headers['userid'] !== 'user1') {
        throw new Error('Unauthorized');
      }
	  
	  6. Добавление кода проброса UserId параметра из API ApolloGateway в сабграфы
	   buildService: ({ name, url }) => {
			return new RemoteGraphQLDataSource({
			url,
			willSendRequest({ request, context }) {
			  const userId = request.variables?.userId;
			  
			  if (userId) {
				request.http.headers.set('userid', userId);
			  } 
			}
    });
	
	7. Заполнение резолверов в hotel-subgraph заглушками для Hotel & Query hotelsByIds